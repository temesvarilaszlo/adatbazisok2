a)
WRITE(A, 10); OUTPUT(A); <ABORT, T>, <ABORT, U>; FLUSH LOG

b)
WRITE(A, 11); OUTPUT(A); WRITE(C, 30); OUTPUT(C); WRITE(A, 10); OUTPUT(A); <ABORT, T>; FLUSH LOG

c)
WRITE(E, 50); OUTPUT(E); WRITE(A, 11); OUTPUT(A); WRITE(C, 30); OUTPUT(C); WRITE(A, 10); OUTPUT(A); <ABORT, T>; <ABORT, V>; FLUSH LOG 

d)
WRITE(F, 15); OUTPUT(F); <ABORT, V>; FLUSH LOG

e)
-